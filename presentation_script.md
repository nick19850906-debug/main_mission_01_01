# 🎙️ 포트폴리오 프로젝트 최종 발표 대본 (루시드프론트)

> **💡 발표 진행 가이드**:
> - **발표자**: 루시드프론트 (신입 프론트엔드 개발자)
> - **목표 소요 시간**: 약 4~5분
> - **핵심 포인트**: 화려한 시각 효과보다 **과제에서 요구한 핵심 기술적 구현 사항(웹 표준, 웹 접근성, 반응형 레이아웃, 단방향 상태 관리, 비동기 통신 4대 상태 처리, 폼 검증, 성능 최적화)**을 논리정연하게 증명하는 데 집중합니다.
> - `[시연 행동]`으로 표시된 부분에 맞춰 브라우저 화면을 직접 조작하며 시연하세요.

---

## 1. 도입 및 프로젝트 개요 (Hero 섹션 노출)

> **[시연 행동]**:  
> 브라우저 화면 상단(Hero 섹션)을 띄워둔 상태에서, 차분하고 자신감 있는 톤으로 발표를 시작합니다.

"안녕하십니까. **'도구에 휘둘리지 않고, 브라우저의 기본 원리로 웹을 이해하는 개발자'**, 루시드프론트입니다.

제가 이번에 구축한 포트폴리오 웹사이트는 React나 Vue, Tailwind 같은 외부 프레임워크나 라이브러리에 일절 의존하지 않고, 오직 **순수 HTML5, CSS3, ES6+ JavaScript**만으로 제작되었습니다.

이 프로젝트의 가장 핵심적인 목표는 **'프레임워크 뒤에 가려진 브라우저의 본질적인 동작 원리를 깊이 체득하는 것'**이었습니다. 
웹 표준과 웹 접근성을 철저히 준수한 마크업부터, 모바일 퍼스트 반응형 레이아웃, 그리고 최신 프레임워크의 심장인 **'이벤트 발생 → 상태 변경 → DOM 렌더링'**의 단방향 데이터 흐름을 바닐라 자바스크립트로 온전히 구현해 냈습니다.

*(배경의 은은한 파티클과 부드러운 스크롤을 살짝 보여주며)*  
참고로 배경에는 브라우저의 동적 렌더링 역량을 입증하기 위해 Canvas API 기반의 파티클과 모멘텀 스크롤 엔진을 직접 구현해 두었지만, 오늘 발표에서는 **과제에서 요구한 웹의 핵심 기본기와 기능적 완성도**를 중심으로 설명해 드리겠습니다."

---

## 2. 웹 표준 시맨틱 마크업과 웹 접근성(a11y) (About 섹션으로 스크롤)

> **[시연 행동]**:  
> 마우스 휠 또는 네비게이션의 [About]을 클릭하여 부드럽게 스크롤합니다.  
> 개발자 도구(F12)의 Elements 탭을 살짝 보여주거나 화면 요소를 가리킵니다.

"첫 번째로 **웹 표준 시맨틱 태그 설계와 웹 접근성(Accessibility)**입니다.

과거의 관행처럼 무의미한 `<div>` 태그를 남용하지 않고, 문서의 논리적 위계에 맞춰 시맨틱 태그를 엄격히 적용했습니다.
- 상단 네비게이션은 `<header>`와 `<nav>`,
- 페이지 전체에서 핵심 주제를 담는 영역은 단 하나만 존재하는 `<main>`,
- 각 주제별 구역은 `<section>`,
- 독립적으로 재사용 가능한 개별 카드 요소들은 `<article>`,
- 하단 정보는 `<footer>`로 구조화했습니다.

이는 검색 엔진 로봇이 콘텐츠를 정확히 색인하도록 돕는 **SEO 최적화**뿐만 아니라, 스크린 리더기를 사용하는 시각장애인 사용자에게 필수적인 구조입니다.

특히 웹 접근성을 위해 다음 4가지 사항을 철저히 검증했습니다:
1. 키보드 사용자를 위한 **최상단 스킵 네비게이션(`skip-link`)**을 배치하여 본문으로 즉시 이동할 수 있게 했습니다.
2. 모든 대화형 버튼에는 `aria-label`, 메뉴 열림 여부를 알리는 `aria-expanded`, 테마 상태를 나타내는 `aria-pressed`를 실시간으로 동기화했습니다.
3. 모바일 터치 환경을 고려하여 모든 인터랙션 요소는 **최소 터치 표적 크기인 44×44px 이상**을 확보했습니다.
4. 이미지에는 의미 있는 `alt` 대체 텍스트를, 입력 폼에는 명시적인 `<label for>`를 완벽히 매핑했습니다."

---

## 3. CSS3 반응형 아키텍처: Flexbox vs Grid & 다크모드 (Skills 섹션)

> **[시연 행동]**:  
> 1) 브라우저 가로폭을 줄였다 늘리며 프로젝트 카드의 반응형 줄바꿈을 시연합니다.  
> 2) 우측 상단의 테마 토글 버튼을 클릭하여 다크 모드와 라이트 모드를 전환합니다.

"두 번째는 **CSS 레이아웃 전략과 테마 아키텍처**입니다.

저는 화면을 설계할 때 **'1차원 정렬인가, 2차원 바둑판 정렬인가'**라는 명확한 기술적 근거를 바탕으로 Flexbox와 CSS Grid를 분리 적용했습니다.
- 상단 네비게이션 헤더(`.header-container`)는 좌측 로고와 우측 메뉴를 단일 축에서 양 끝으로 배치해야 하므로 **1차원 Flexbox(`justify-content: space-between`)**를 사용했습니다.
- 반면 프로젝트 카드 목록(`.projects-grid`)은 가로와 세로를 동시에 다루는 **2차원 CSS Grid**를 적용했습니다. 특히 `grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));` 속성을 작성하여, 복잡한 미디어 쿼리를 일일이 쓰지 않고도 카드가 최소 280px을 유지하며 화면 너비에 맞춰 1열, 2열, 3열로 스스로 유연하게 줄바꿈되도록 구현했습니다.

또한 **모바일 퍼스트(Mobile-First)** 원칙에 따라 기본 스타일을 모바일에 맞추고, `768px`(태블릿), `1024px`(데스크톱) 브레이크포인트로 확장했습니다.

지금 보시는 **다크 모드 시스템**은 CSS 커스텀 속성(Variables)으로 구축되었습니다.  
`:root`와 `[data-theme="dark"]`에 색상 토큰을 체계적으로 정의하여, 버튼 클릭 시 최상단 속성 하나만 교체되면 하위 모든 컴포넌트가 일괄 반응합니다. 사용자의 선택은 `localStorage`에 영구 보존되며, 방문자의 OS 시스템 테마(`prefers-color-scheme`)도 초기에 자동 감지하도록 설계했습니다."

---

## 4. 자바스크립트 아키텍처: 단방향 상태 관리 (Mental Model)

> **[시연 행동]**:  
> 개발자 도구의 콘솔(Console) 탭을 열어, 테마를 바꾸거나 필터를 누를 때 찍히는  
> `%c[State Changed]` 로그를 보여줍니다.

"세 번째는 자바스크립트의 핵심인 **'단방향 상태 관리 아키텍처'**입니다.

바닐라 자바스크립트 프로젝트에서 흔히 저지르는 실수는, 버튼을 누를 때마다 DOM 요소를 마구잡이로 직접 수정하여 스파게티 코드가 되는 것입니다.  
저는 React나 Vue의 핵심 멘탈 모델인 **'이벤트(Event) 발생 → 중앙 상태(State) 변경 → 화면(DOM) 렌더링'** 패턴을 바닐라 JS로 완벽히 정립했습니다.

1. 모든 전역 상태는 `AppState`라는 단 하나의 중앙 객체에서 관리합니다.
2. 상태를 변경할 때는 불변성(Immutability)을 보장하는 `setState()` 헬퍼 함수를 통해서만 갱신되며, 변경 전후의 상태가 콘솔에 명확히 로깅됩니다.
3. 화면은 오직 변경된 상태 데이터를 바라보고 순수하게 다시 렌더링됩니다.

또한 HTML 내에 `onclick` 같은 인라인 속성을 일절 배제하여 **관심사 분리(Separation of Concerns)**를 지켰고, 이벤트 리스너에는 명명된 함수를 바인딩하여 메모리 누수를 방지하고 재사용성을 확보했습니다.  
데이터 변환 시에는 `map`, `filter`, `forEach` 같은 ES6+ 고차 배열 함수와 구조분해 할당을 적극 활용하여 가독성과 안정성을 극대화했습니다."

---

## 5. GitHub API 비동기 통신과 4대 상태 UI 분기 (Projects 섹션)

> **[시연 행동]**:  
> Projects 섹션에서:  
> 1) 상단 언어 필터 버튼(HTML, CSS, JavaScript)을 클릭하여 카드 필터링 시연  
> 2) 검색창에 잘못된 아이디(예: `asdfqwer12344321`)를 입력하고 검색하여 **에러 UI** 노출  
> 3) **[다시 시도]** 버튼을 클릭하여 복구 시연

"네 번째는 실제 프로덕션 수준의 **GitHub API 비동기 통신과 4대 상태 UI 처리**입니다.

서버와 통신할 때는 `fetch` API와 `async/await`, 그리고 `try/catch` 문을 사용하여 예외 처리를 빈틈없이 구현했습니다. 실제 웹 서비스에서 사용자 경험(UX)을 결정짓는 것은 네트워크 통신의 **'4가지 상태(State)'**를 화면에 어떻게 보여주느냐입니다.
1. **로딩(Loading) 상태**: 데이터를 요청하는 즉시 회전하는 로딩 스피너를 띄워 사용자가 작업 진행 중임을 인지하게 합니다.
2. **성공(Success) 상태**: HTTP 200 응답이 오면 JSON 데이터를 파싱하고 `map()`을 순회하여 프로젝트 카드를 동적으로 렌더링합니다.
3. **에러(Error) 상태**: 네트워크 단절, 존재하지 않는 계정, 또는 GitHub API의 시간당 호출 제한(403 Rate Limit)이 발생하면 화면이 멈추지 않고, 친절한 에러 안내 문구와 함께 즉시 재요청할 수 있는 **[다시 시도]** 버튼을 띄웁니다.
4. **빈(Empty) 상태**: 호출은 성공했으나 저장소가 0개일 때는 '등록된 프로젝트가 없습니다'라는 명확한 빈 상태 박스를 제공합니다.

더 나아가, 모바일 열악한 네트워크 환경에 대비하여 `AbortController`를 연동한 **8초 타임아웃 방어 로직**과 **최대 3회 자동 재시도(Retry)** 로직까지 구현하여 시스템 안정성을 강화했습니다."

---

## 6. 폼 실시간 유효성 검사 & 성능 최적화 (Contact 섹션)

> **[시연 행동]**:  
> Contact 폼으로 이동하여:  
> 1) 아무것도 입력하지 않고 [메시지 전송]을 눌러 **인라인 에러 메시지**와 붉은색 테두리 노출 시연  
> 2) 이메일에 잘못된 형식(예: `abc@com`)을 입력하여 실시간 정규식 에러 피드백 시연  
> 3) 올바른 값 입력 후 성공 전송 토스트 알림 배너 시연

"마지막으로 **Contact 폼 실시간 유효성 검사**와 **브라우저 성능 최적화**입니다.

폼 검증은 단순히 제출할 때만 확인하는 것이 아니라, 사용자가 입력을 마치는 즉시 피드백을 주는 **실시간 밸리데이션(Input & Blur)**을 적용했습니다.
- 필수값 누락 검사는 물론, 이메일은 엄격한 정규표현식(Regex)을 통해 올바른 형식인지 검증합니다.
- 검증에 실패하면 브라우저 기본 경고창(`alert`) 대신, 입력창 하단에 명확한 **인라인 에러 메시지**를 띄우고 첫 번째 에러 입력창으로 포커스를 자동 이동시킵니다.
- 스크린 리더기를 위해 `aria-describedby`로 에러 메시지와 입력창을 논리적으로 연결했습니다.

또한 페이지 전반의 렌더링 성능을 위해:
- 스크롤 이벤트에는 **100ms 간격의 Throttle(쓰로틀링)**을 적용하여 브라우저 메인 스레드 부하를 최소화했고,
- 스크롤 등장 애니메이션은 `Intersection Observer API`를 활용하여 뷰포트에 한 번 들어온 요소는 즉시 관찰을 해제(`unobserve`)함으로써 모바일 저사양 기기에서도 60fps의 매끄러운 성능을 유지하도록 최적화했습니다."

---

## 7. 마무리 및 Q&A 준비 (클로징)

> **[시연 행동]**:  
> 최하단 푸터까지 화면을 부드럽게 보여준 뒤, 평가관을 응시하며 발표를 맺습니다.

"발표를 마치며 정리하자면, 이번 프로젝트는 시각적인 기교 이전에 **'웹의 3대 기초인 HTML의 시맨틱 구조, CSS의 탄탄한 반응형 레이아웃, 그리고 JavaScript의 상태 기반 단방향 데이터 흐름'**이라는 가장 본질적인 기본기를 철저히 증명한 결과물입니다.

외부 프레임워크나 라이브러리를 사용하면 손쉽게 만들 수 있는 기능들이지만, 이를 브라우저의 순수 API로 직접 설계하고 예외 상황과 성능까지 고민해 본 경험은, 앞으로 제가 React, Next.js 등 어떠한 현대적 프레임워크를 다루더라도 문제의 근본 원인을 파악하고 해결할 수 있는 강력한 무기가 될 것이라 확신합니다.

경청해 주셔서 대단히 감사합니다. 질문 주시면 성심껏 답변드리겠습니다!"

---

## 💡 [보너스] 평가관 예상 돌발 질문 및 핵심 답변 카드 (치트시트)

### Q1. "Flexbox와 Grid를 둘 다 쓰셨는데, 왜 하나로 통일하지 않았나요?"
> **🗣️ 핵심 답변**:  
> "1차원 축 정렬과 2차원 바둑판 정렬의 목적이 완전히 다르기 때문입니다. 상단 네비게이션처럼 좌우 단일 방향 정렬은 **Flexbox**가 가장 직관적이고 가볍습니다. 반면 프로젝트 카드 목록은 화면 폭에 따라 열과 행이 동시에 바뀌어야 하므로 **CSS Grid의 `repeat(auto-fit, minmax())`**를 쓰는 것이 미디어 쿼리 없이도 완벽한 반응형을 구현하는 가장 모던하고 정석적인 방법이기 때문에 목적에 맞게 분리했습니다."

### Q2. "React의 useState 대신 바닐라 JS에서 AppState와 setState를 굳이 만든 이유는?"
> **🗣️ 핵심 답변**:  
> "버튼을 누를 때마다 DOM을 직접 조작(jQuery 방식)하면 프로젝트가 커질수록 어디서 화면이 바뀌었는지 추적하기가 불가능해집니다. 이를 방지하기 위해 **'이벤트 → 상태 변경 → DOM 렌더링'**이라는 단방향 데이터 흐름을 확립하고 불변성(Immutability)을 유지하여, 데이터의 흐름을 예측 가능하고 디버깅하기 쉽게 만들기 위해 설계했습니다."

### Q3. "GitHub API 호출 시 AbortController는 왜 쓰셨나요?"
> **🗣️ 핵심 답변**:  
> "일반적인 `fetch`는 서버 응답이 올 때까지 하염없이 기다리거나 연결이 불안정할 때 브라우저 리소스를 낭비합니다. 이를 방지하기 위해 8초 동안 응답이 없으면 네트워크 요청을 강제로 중단(Abort)하고 사용자에게 타임아웃 에러 및 재시도 UI를 보여주어 사용자 경험을 지키기 위해 적용했습니다."

### Q4. "스크롤 이벤트에 Throttle과 Intersection Observer를 각각 쓴 이유는?"
> **🗣️ 핵심 답변**:  
> "스크롤 이벤트(`window.onscroll`)는 1초에 수십 번씩 발생하여 브라우저 성능을 급격히 떨어뜨립니다. 따라서 헤더 블러 전환 같은 연속 감지에는 **Throttle(100ms)**을 걸어 호출 빈도를 제한했고, 카드 등장 애니메이션은 무거운 스크롤 이벤트 대신 브라우저가 하드웨어 가속으로 화면 진입을 비동기 감지해 주는 **`Intersection Observer API`**를 사용하고 1회 노출 즉시 `unobserve`하여 렌더링 성능을 극대화했습니다."
